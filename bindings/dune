(executable
 (name vm)
 (libraries monad_lib)
 (foreign_stubs
  (language c)
  (include_dirs "../third_party/evmc/include")
  (names vm))
 (flags
  (:standard -linkall))
 (modes
  (native shared_object)))

(env
 (release
   (ocamlopt_flags (:standard -unsafe -noassert -inline 100 -O3 -w -64))))
