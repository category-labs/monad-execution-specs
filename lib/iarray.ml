include Array
(*
type 'a t = 'a array

let init = Array.init

let get = Array.get

let mapi = Array.mapi

let map = Array.map

let find_mapi = Array.find_mapi

let fold_left = Array.fold_left

let to_seq = Array.to_seq

 *)
